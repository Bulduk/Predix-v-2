import AppContainer from '@/components/AppContainer';

export default function Home() {
  return (
    <main className="h-[100dvh] w-full bg-black overflow-hidden flex items-center justify-center">
      <AppContainer />
    </main>
  );
}
